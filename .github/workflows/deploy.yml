name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Серверный роут не поддерживается статическим экспортом.
      # Удаляем его только из статической сборки (в репозитории он остаётся
      # для деплоя на Node/Vercel). На статике форма уходит в Telegram deep-link.
      - name: Remove server API route for static export
        run: rm -rf app/api

      - name: Build static export
        run: npm run build
        env:
          STATIC_EXPORT: "1"

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
